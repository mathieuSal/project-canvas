import React from 'react';
import ReactDOM from 'react-dom';
import GalleryManager from 'Src/app/gallery/galleryManager';

ReactDOM.render(
  <GalleryManager />,
  // eslint-disable-next-line no-undef
  document.getElementById('app'),
);
